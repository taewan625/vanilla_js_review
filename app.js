// conditional 조건문

console.log(typeof parseInt(age)); // JS 이상한 기능

const age = parseInt(prompt("how old are you?")); // JS는 안에서 부터 바깥으로 결과를 도출한다
console.log(age); // JS 이상한 기능

parseInt(); // string -> Inst
